const colors = {
  text: '#111111',
  csBackground: '#f9f9f9',
  black: '#000000',
  white: '#ffffff',
  midGrey: '#7d7d7d',
}

const ease = {
  standard: 'cubic-bezier(0, 0, 0.2, 1)',
}

const duration = {
  standard: 500,
}

const margins = {
  standard: '1rem',
  navDesktop: '4rem',
  section: { md: '8rem' },
}

export default {
  colors,
  ease,
  duration,
  margins,
}
